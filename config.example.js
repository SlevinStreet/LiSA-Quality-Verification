/**
 * LiSA VOC QC Platform — Runtime Configuration Template
 * ─────────────────────────────────────────────────────────────────────────────
 * ✅ This file IS safe to commit — it contains no real credentials.
 *
 * To set up locally:
 *   cp config.example.js config.js
 *   Then replace the placeholder values in config.js with your real credentials.
 *
 * Where to find your credentials:
 *   - baseUrl: Your InsForge project URL (InsForge Dashboard → Project Settings)
 *   - anonKey: Your project's anon/public key (InsForge Dashboard → API Keys)
 * ─────────────────────────────────────────────────────────────────────────────
 */
window.__LISA_CONFIG__ = {
  baseUrl: 'https://your-project.region.insforge.app',
  anonKey: 'your-anon-key-here'
};
